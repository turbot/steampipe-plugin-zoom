connection "zoom" {
  plugin = "zoom"

  # Zoom API credentials are available to users with Developer role in
  # the account.
  # zoom_api_key    = "YOUR_API_KEY"
  # zoom_api_secret = "YOUR_API_SECRET"
}
